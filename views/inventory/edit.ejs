<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Item</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

<nav>
  <div class="logo">POS Strategy System</div>
  <div>
    <a href="/dashboard">Dashboard</a>
    <a href="/inventory">Inventory</a>
    <a href="/logout">Logout</a>
  </div>
</nav>

<div class="center-box">
  <h1>Edit Item</h1>

  <form action="/inventory/edit/<%= item.id %>" method="POST">
    <input type="text" name="product_name" placeholder="Item Name" value="<%= item.product_name %>" required>
    <input type="number" name="quantity" placeholder="Stock" value="<%= item.quantity %>" required>
    <input type="number" step="0.01" name="cost_price" placeholder="Cost Price" value="<%= item.cost_price %>" required>
    <input type="number" step="0.01" name="selling_price" placeholder="Selling Price" value="<%= item.selling_price %>" required>

    <button type="submit">Update Item</button>
  </form>

  <a class="small-link" href="/inventory">Back</a>
</div>

</body>
</html>
